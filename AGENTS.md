# CDP北海道システム開発ルール

## UI・UX変更

- バックエンドのみ、または表示を変えない文言修正を除く、すべてのUI・UX変更では必ず `$cdp-ui-design` を使用する。
- 実装前に同Skillの `references/ui-quality-gate.md` を読み、操作対象と操作結果が近接しているか確認する。
- 選択した項目の詳細を一覧全体の下へ表示する、小さなボタンを広い空白内に孤立させる、重要情報を薄い小文字で表示する設計は禁止する。
- 公開前に1440px、1024px、390pxで実操作とスクリーンショット確認を行う。文字切れ、不要な折り返し、横スクロール、遠隔位置への表示、フォーカス消失が残る場合は公開しない。

## 情報保護・外部連携

- 実在する個人名、企業の非公開情報、電話番号、メールアドレス、顧客データ、パスワード、APIキー、OAuthトークン、秘密鍵を、Figma、AIサービス、公開サイト、公開リポジトリ、第三者プラグインへ入力・送信・保存しない。画面設計にはダミーデータだけを使用する。
- 外部サービスによる読み取り、アップロード、送信、公開、共有設定変更、OAuth接続、プラグイン実行は、対象データ・送信先・目的を提示し、ユーザーの明示承認後にだけ実行する。連携権限は常に `Always ask` を標準とする。
- GitHub Pagesへ公開するファイルに秘密値や実データを含めない。公開前に作業ツリーとGit履歴の両方を秘密情報・個人情報パターンで検査する。
- フォーム本文や連絡先をURLのクエリ文字列・フラグメントへ入れない。デモの同一タブ内引き渡しは `sessionStorage` を使用し、処理後すぐ削除する。本番保存は認証・権限制御・暗号化・監査ログを備えたサーバー側で行う。
- 外部CDN、解析タグ、外部フォント、第三者JavaScript、Figma Communityプラグインは、出所・送信先・ネットワーク権限を確認できない限り追加しない。
- 誤って作成した画面・機能・通信処理は、非表示で残さず不要コードごと削除する。削除対象がユーザーデータを含む場合は、削除前に対象を明示して承認を得る。
- ChatGPTやFigmaの学習設定・メモリ設定だけを安全対策とみなさない。外部送信前の承認、最小権限、ダミーデータ、公開前検査を必須の防御とする。
